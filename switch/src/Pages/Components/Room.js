import React, { Component } from 'react';

function Room(props){
    return(
        <button className="room-box" onClick={props.onClick}>
            
        </button>
    );
}

export default Room;